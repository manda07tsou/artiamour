<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>"> #}
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset("dist/app.css") }}">
            <link rel="stylesheet" href="{{ asset("dist/admin.css") }}">
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}
    </head>
    <body>
        <div class="admin-header">
            <a href="?" class="logo">
                <img src="{{ asset("dist/logo1.jpg") }}" alt="logo">
            </a>

            <drop-down class="js-dropdown">
                <a href="#categorie" class="dropdown-toggle" id="category-link"><i class="fas fa-user-circle mr-1"></i>artiamour@gmail.com</a>
                <div class="dropdown-content box" hidden>
                    <div class="text-center" style="font-size: 2em; line-height:1.4em">
                        <i class="fas fa-user-circle mr-1"></i>
                    </div>
                    <div class="line"></div>
                    <a href="#">Paramètre compte</a>
                    <div class="line"></div>
                    <a href="#">Déconnéxion</a>
                </div>
            </drop-down>
        </div>

        {% block body %}
        {% endblock %}

        {% block javascripts %}
            <script src="{{ asset("dist/app.js")}}"></script>
            <script src="{{ asset("dist/admin.js")}}"></script>
        {% endblock %}
    </body>
</html>
