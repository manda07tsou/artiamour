{% extends 'admin/base.html.twig' %}

{% block title %}Changer mot de passe!{% endblock %}

{% block body %}
    <div class="container my-3">
        <h4 class="section-title text-left">Mon compte</h4>
        <div class="grid-2">
            {{ form_start(form)}}
                <div class="">
                    {{ form_row(form.password.first)}}
                </div>
                    <div class="mt-2">
                    {{ form_row(form.password.second)}}
                </div>
                {# <div class="mt-2">
                    <input type="checkbox">
                    <label>Afficher mot de passe</label>
                </div> #}
                <div class="mt-2">
                    <button type="sumbit" class="btn-secondary">Modifier</button>
                </div>
            {{ form_end(form)}}
        </div>
    </div>
{% endblock %}
